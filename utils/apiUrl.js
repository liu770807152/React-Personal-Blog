const baseUrl = 'localhost:7001/api/';
const servicePath = {
  articleList: '/api/articleList', // interface URL for /list page
  article: '/api/article' // interface URL for /detail page
};

export default servicePath;
